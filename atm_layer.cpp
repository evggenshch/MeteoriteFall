#include "atm_layer.h"

atm_layer::atm_layer()
{
}

atm_layer::atm_layer(double set_density, double set_height, double set_gamma)
{
    density = set_density;
    height = set_height;
    gamma = set_gamma;
}
